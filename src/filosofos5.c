#include <stdio.h>
#include <pthread.h>
#include <filosofos.h>

void *filosofo(void * arg){
    struct Filosofo * filosofo = (void *) arg;
}

void ejecutar(){

    struct Filosofo filosofos[numeroFilosofos];
}